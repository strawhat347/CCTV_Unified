# ==============================================================================
# CCTV Unified Surveillance MVP - GPU Accelerated Requirements (EXPERIMENTAL)
#
# WARNING: PyTorch GPU + PaddlePaddle GPU in the same process is CONFIRMED
# TO CRASH on Windows with `OSError: [WinError 127]` (DLL collision between
# bundled cuDNN copies). Tested 2026-08-28.
#
# Root cause is a same-process .dll symbol collision, NOT missing kernels —
# nightly PaddlePaddle confirmed sm_120/Blackwell support via
# paddle.utils.run_check() -> "PaddlePaddle works well on 1 GPU".
#
# DO NOT use this file for the stable/production pipeline. That is
# requirements-cpu.txt (YOLO on GPU via torch, OCR on CPU via paddlepaddle).
# Only install this in an isolated/disposable venv for testing, or once
# process-isolated (multiprocessing/IPC) OCR is implemented.
# ==============================================================================

# --- PyTorch (Blackwell/sm_120 requires nightly cu128) ---
--pre
--extra-index-url https://download.pytorch.org/whl/nightly/cu128
torch==2.12.0.dev20260408+cu128
torchvision==0.27.0.dev20260407+cu128
torchaudio==2.11.0.dev20260407+cu128

# --- PaddlePaddle GPU (Blackwell/sm_120 requires nightly cu126) ---
--pre
--extra-index-url https://www.paddlepaddle.org.cn/packages/nightly/cu126/
paddlepaddle-gpu==3.5.0.dev20260525
paddleocr>=2.7.0

# --- Web Framework & API Layer ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
slowapi>=0.1.9
starlette>=0.36.0
python-multipart>=0.0.9
websockets>=12.0
python-dotenv>=1.0.0
httpx>=0.27.0
requests>=2.31.0

# --- Database & Storage Layer ---
mysql-connector-python>=8.3.0
SQLAlchemy>=2.0.0
pandas>=2.2.0
numpy>=1.26.0,<2.0.0

# --- Computer Vision (non-DL-framework) ---
ultralytics>=8.1.0
opencv-contrib-python>=4.10.0
pillow>=10.2.0
shapely>=2.0.0
pyclipper>=1.3.0
scikit-image>=0.22.0
scipy>=1.12.0
RapidFuzz>=3.6.0

# --- Presentation & Desktop Shell ---
pywebview>=4.4.1
cryptography>=42.0.0
psutil>=5.9.0

# --- Utilities & Testing ---
pytest>=8.0.0
tqdm>=4.66.0
PyYAML>=6.0.1